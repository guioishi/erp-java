spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/erp
    username: erp
    password: erp
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080
